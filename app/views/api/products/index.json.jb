# File previously named all_products.json.jb
@products.map do |product|
  {
    name: product.name,
    price: product.price,
    image_url: product.image_url,
    description: product.description,
    id: product.id
  }
end